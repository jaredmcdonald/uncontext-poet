extends layout

block content
  aside#controls
    h4 algorithm editor
    textarea#editor m / s
    pre#sample
  pre#container(data-parsed=JSON.stringify(item.parsed))
  script(src='/socket.io/socket.io.js')
  script(src='/js/dist/app.js')
